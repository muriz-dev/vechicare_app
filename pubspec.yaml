name: vechicare_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  auto_route: ^11.1.0
  get_it: ^9.2.1
  injectable: ^3.0.0
  fl_chart: ^1.2.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  build_runner: ^2.15.0
  injectable_generator: ^3.1.0

flutter:
  uses-material-design: true
